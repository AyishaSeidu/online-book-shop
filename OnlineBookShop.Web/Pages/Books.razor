@page "/"
@inherits BooksBase

@if(Books ==null)
{
    <div >
    <h3>Loading books... </h3>
    <Spinner/>
    </div>
}
else 
{
    <h3 class="mb-5" >Showing all books</h3>

    var numOfBooks = Books.Count();

    if(numOfBooks>0)
    {

        var itemRowList = new List<BookReadDTO>();

@*        @foreach (var bookGroup in GetGroupedBooksByGenre())
        {
            <h4>@GetGenreName(bookGroup)</h4>*@

            <div class="row mt-3">
                <DisplayBooks Books ="@Books" ></DisplayBooks>

            </div>
        @*}*@
    }
}
